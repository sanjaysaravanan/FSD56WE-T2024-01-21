// Iterating over an array
const arr = ["Sam", "Bala", "Sanjay", "Balaji", "Alamat"];

let i = 0;
while (i < arr.length) {
  console.log(arr[i]);
  i++;
}

// Iterating over a string
const str = "FSD-56-WE-T";

let i = 0;
while (i < str.length) {
  console.log(str[i]);
  i++;
}
